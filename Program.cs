LinqQueries queries = new();

// Get all books
// queries.GetAllBooks();

// Get books published after a specific year
// queries.GetBooksAfterYear(2010);

// Get books with a specific title and page count
// queries.GetBooksWithSpecificTitleAndPageCount("android", 200);

// Verify if all books have a status
// bool allBooksHaveStatus = queries.AllBooksHaveStatus();
// Console.WriteLine($"All books have a status? - {allBooksHaveStatus}");

// Check if any book was released in a specific year
// bool anyBookReleasedInYear = queries.AnyBookReleasedInSpecificYear(2009);
// Console.WriteLine($"Any book released in 2015? - {anyBookReleasedInYear}");